extends layout.pug
block content
    sections#content
        ul
            each color in colors
                li= color
        h2= prompt
        if  hint
            p
                i Hint: #{hint}
    
